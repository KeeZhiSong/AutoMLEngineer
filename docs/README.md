# docs/

| File | Status |
|---|---|
| `CLAUDE.md` | Original design brief. **Partly superseded** — written before the Starter Kit; its task definition (AliCCP, `is_click`, NDCG@10/Recall@50) is wrong. Architecture section still holds. See the header. |

Current, correct references live at the repo root: `README.md` for the contract
and layout, `BASELINE.md` for the baseline reproduction and the organisers'
measured dead ends.
